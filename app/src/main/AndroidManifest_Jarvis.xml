<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jarvis.app">

    <!-- الإنترنت مطلوب فقط للمزامنة في الخلفية — كل شيء آخر يشتغل بدونه -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.JarvisX"
        android:supportsRtl="true">

        <!-- نقطة الدخول الوحيدة: هي لي تقرر توجّهك لـ Admin ولا Onboarding ولا Member -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.JarvisX.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==================== Admin ==================== -->
        <activity
            android:name=".admin.AdminMainActivity"
            android:exported="false" />

        <activity
            android:name=".admin.GroupChatManagerActivity"
            android:exported="false"
            android:label="إدارة الدردشة الجماعية" />

        <!-- ==================== Family / Member ==================== -->
        <activity
            android:name=".onboarding.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".member.MemberMainActivity"
            android:exported="false" />

        <activity
            android:name=".member.GroupChatActivity"
            android:exported="false"
            android:label="الدردشة الجماعية" />

    </application>

</manifest>
